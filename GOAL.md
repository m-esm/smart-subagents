# GOAL (edit freely)

One backlog of Claude subagent-feature practices from current articles and X, with those items implemented rather than left as research. Done means: practices land in `design/roadmap/` with a current article, X post, or vendor-docs URL and ship (`state: shipped`, measured `after:` on trunk), not as research notes. It is explicitly NOT a general multi-agent framework, not a replacement for the parent's review of the final diff, and never labor left as unimplemented research.

## Numbers that prove it
- shipped sourced practice rows: `python3 scripts/measure_shipped_practices.py` - today: 2; target: 8

source: design/roadmap/*.md, scripts/measure_shipped_practices.py
