# CGC-SKIP examples

Legal implement-brief skip (`dispatch` requires `route=` and `evidence=`):

CGC-SKIP: docs-only, no code graph; route=ast-grep; evidence=ast-grep -p 'CGC-SKIP:' docs/

Claude cannot read `$DIR/brief.md` inside the sandbox. Copy it into the worktree as `BRIEF.md` (or use grok/codex).
`dispatch` already stages `$WT/.ssa-brief.md` as a file-ref launch path; `$DIR/brief.md` remains the supervisor artifact.
